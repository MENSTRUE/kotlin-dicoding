
    private fun sum(valueA: Int, valueB: Int): Int{
    // another magic
        return valueA + valueB
    }

    /*
   { private ==> (visibility modifier) fun sum ==> (function name)(valueA: Int, valueB: Int) ==> (funtion parameter) : Int ==> (function return type)  } ==> function header{
    // another magic ==> function body
        return valueA + valueB
    }
     */